<mvc:View
    controllerName="o2c.controller.OrderCreate"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:f="sap.ui.layout.form"
    xmlns:core="sap.ui.core">

    <Page title="{i18n>pageOrderCreate}">
        <customHeader>
            <Bar>
                <contentLeft>
                    <Button icon="sap-icon://nav-back" press=".navBack" />
                    <Title text="{i18n>pageNewOrder}" />
                </contentLeft>
                <contentRight>
                    <Button text="{i18n>btnSave}" type="Emphasized" icon="sap-icon://save" press=".onSaveOrder" />
                    <Button text="{i18n>labelDiscard}" type="Default" press=".navBack" />
                </contentRight>
            </Bar>
        </customHeader>

        <content>
            <VBox class="sapUiMediumMargin">

                <!-- Order Header -->
                <Panel headerText="{i18n>labelOrderHeader}" expandable="false" class="sapUiSmallMarginBottom">
                    <f:SimpleForm editable="true" layout="ColumnLayout" columnsL="2" columnsM="2">
                        <f:content>
                            <Label text="{i18n>colOrderId}" />
                            <Input value="{/newOrder/OrderId}" enabled="false" />

                            <Label text="{i18n>colCustomer}" required="true" />
                            <Select id="orderCustomer" change=".onCustomerChange"
                                    selectedKey="{/newOrder/CustomerId}">
                                <core:Item key="" text="{i18n>selectCustomer}" />
                                <items>
                                    <core:ListItem key="{CustomerId}" text="{CustomerName}"
                                                   additionalText="{CustomerId}"
                                                   xmlns:core="sap.ui.core" />
                                </items>
                            </Select>

                            <Label text="{i18n>colOrderDate}" required="true" />
                            <DatePicker id="orderDate" value="{/newOrder/OrderDate}"
                                        displayFormat="yyyy-MM-dd" valueFormat="yyyy-MM-dd" />

                            <Label text="{i18n>colOrderStatus}" />
                            <Input value="Draft" enabled="false" />

                            <Label text="{i18n>colCreditStatus}" />
                            <Input value="Pending" enabled="false" />
                        </f:content>
                    </f:SimpleForm>
                </Panel>

                <!-- Customer Credit Info -->
                <Panel id="creditInfoPanel" headerText="{i18n>labelCustomerCreditInfo}"
                       expandable="false" class="sapUiSmallMarginBottom" visible="false">
                    <HBox class="sapUiSmallMargin" wrap="Wrap">
                        <VBox class="sapUiSmallMarginEnd">
                            <Label text="{i18n>colCreditLimit}" />
                            <Text text="{/newOrder/_creditLimit}" class="sapUiSmallMarginTop" />
                        </VBox>
                        <VBox class="sapUiSmallMarginEnd">
                            <Label text="{i18n>colUsedCredit}" />
                            <Text text="{/newOrder/_usedCredit}" class="sapUiSmallMarginTop" />
                        </VBox>
                        <VBox class="sapUiSmallMarginEnd">
                            <Label text="{i18n>colAvailable}" />
                            <Text text="{/newOrder/_availCredit}" class="sapUiSmallMarginTop" />
                        </VBox>
                    </HBox>
                </Panel>

                <!-- Order Items -->
                <Panel headerText="{i18n>labelOrderLineItems}" expandable="false" class="sapUiSmallMarginBottom">
                    <headerToolbar>
                        <Toolbar>
                            <Title text="{i18n>colLineItems}" />
                            <ToolbarSpacer />
                            <Button text="{i18n>btnAddProduct}" icon="sap-icon://add" type="Default" press=".onAddItem" />
                        </Toolbar>
                    </headerToolbar>

                    <Table id="itemsTable" items="{/newOrder/items}"
                           noDataText="No items added. Click 'Add Item' to begin."
                           mode="None">
                        <columns>
                            <Column><Text text="{i18n>colProduct}" /></Column>
                            <Column hAlign="Center"><Text text="{i18n>colQuantity}" /></Column>
                            <Column hAlign="End"><Text text="Unit Price (â‚¹)" /></Column>
                            <Column hAlign="End"><Text text="Item Total (â‚¹)" /></Column>
                            <Column hAlign="Center"><Text text="Remove" /></Column>
                        </columns>
                        <items>
                            <ColumnListItem>
                                <cells>
                                    <ObjectIdentifier title="{ProductName}" text="{ProductId}" />
                                    <Input type="Number" value="{Quantity}"
                                           change=".onQtyChange" width="80px" />
                                    <ObjectNumber number="{path:'UnitPrice', formatter:'.formatCurrency'}" />
                                    <ObjectNumber number="{path:'ItemTotal', formatter:'.formatCurrency'}"
                                                  state="Success" />
                                    <Button icon="sap-icon://decline" type="Transparent"
                                            press=".onRemoveItem" />
                                </cells>
                            </ColumnListItem>
                        </items>
                    </Table>

                    <!-- Total Row -->
                    <HBox justifyContent="End" class="sapUiSmallMargin">
                        <VBox alignItems="End">
                            <Label text="Order Total:" design="Bold" />
                            <Title text="{path: '/newOrder/TotalAmount', formatter: '.formatCurrency'}"
                                   titleStyle="H4" />
                        </VBox>
                    </HBox>
                </Panel>

            </VBox>
        </content>

        <!-- Add Item Dialog -->
        <dependents>
            <Dialog id="addItemDialog" title="{i18n>dialogAddItem}" contentWidth="500px">
                <content>
                    <VBox class="sapUiSmallMargin">
                        <Label text="Select Product" required="true" />
                        <Select id="itemProduct" class="sapUiSmallMarginBottom"
                                change=".onProductSelect">
                            <core:Item key="" text="-- Select Product --" />
                        </Select>

                        <Label text="Available Stock" />
                        <Text id="itemStock" text="-" class="sapUiSmallMarginBottom" />

                        <Label text="Unit Price (â‚¹)" />
                        <Input id="itemUnitPrice" enabled="false" class="sapUiSmallMarginBottom" />

                        <Label text="{i18n>colQuantity}" required="true" />
                        <Input id="itemQty" type="Number" placeholder="{i18n>phQuantity}"
                               change=".onItemQtyChange" class="sapUiSmallMarginBottom" />

                        <Label text="Item Total (â‚¹)" />
                        <Input id="itemTotal" enabled="false" />
                    </VBox>
                </content>
                <beginButton>
                    <Button text="{i18n>btnAddToOrder}" type="Emphasized" press=".onConfirmAddItem" />
                </beginButton>
                <endButton>
                    <Button text="{i18n>btnCancel}" press=".onCancelAddItem" />
                </endButton>
            </Dialog>
        </dependents>
    </Page>
</mvc:View>

