<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:f="sap.ui.layout.form">

    <Dialog
        id="customerEditDialog"
        title="Edit Customer â€” {/editCustomer/CustomerId}"
        contentWidth="640px"
        draggable="true"
        resizable="true"
        state="None">

        <content>
            <VBox class="sapUiSmallMarginBeginEnd sapUiSmallMarginTopBottom">

                <f:SimpleForm
                    editable="true"
                    layout="ColumnLayout"
                    columnsL="2"
                    columnsM="2"
                    columnsXL="2"
                    labelSpanL="12"
                    labelSpanM="12">
                    <f:content>

                        <Label text="{i18n>colCustomerId}" />
                        <Input value="{/editCustomer/CustomerId}" enabled="false" />

                        <Label text="{i18n>colCustomerName}" required="true" />
                        <Input
                            id="editName"
                            value="{/editCustomer/CustomerName}"
                            placeholder="{i18n>phCustomerName}" />

                        <Label text="{i18n>labelEmailAddress}" required="true" />
                        <Input
                            id="editEmail"
                            value="{/editCustomer/Email}"
                            type="Email"
                            placeholder="Enter email address" />

                        <Label text="{i18n>labelPhoneNumber}" required="true" />
                        <Input
                            id="editPhone"
                            value="{/editCustomer/Phone}"
                            type="Tel"
                            placeholder="Enter phone number" />

                        <Label text="{i18n>colAddress}" />
                        <TextArea
                            id="editAddress"
                            value="{/editCustomer/Address}"
                            rows="3"
                            placeholder="Enter address" />

                        <Label text="Credit Limit (â‚¹)" />
                        <Input
                            id="editCreditLimit"
                            value="{/editCustomer/CreditLimit}"
                            type="Number"
                            placeholder="Enter credit limit" />

                        <Label text="Used Credit (â‚¹)" />
                        <Input value="{/editCustomer/UsedCredit}" enabled="false" />

                        <Label text="{i18n>colStatus}" />
                        <Select selectedKey="{/editCustomer/Status}">
                            <core:Item key="Active" text="{i18n>statusActive}" />
                            <core:Item key="On Hold" text="{i18n>statusOnHold}" />
                            <core:Item key="Inactive" text="{i18n>statusInactive}" />
                        </Select>

                    </f:content>
                </f:SimpleForm>

                <!-- Credit Utilization Panel -->
                <Panel headerText="Credit Utilization" class="sapUiSmallMarginTop">
                    <HBox class="sapUiSmallMargin" alignItems="Center">
                        <VBox width="100%">
                            <HBox justifyContent="SpaceBetween" class="sapUiTinyMarginBottom">
                                <Label text="Used: â‚¹{/editCustomer/UsedCredit}" />
                                <Label text="Limit: â‚¹{/editCustomer/CreditLimit}" />
                            </HBox>
                            <ProgressIndicator
                                percentValue="{path: '/editCustomer', formatter: '.calcCreditPercent'}"
                                displayValue="{path: '/editCustomer', formatter: '.calcCreditPercentText'}"
                                state="{path: '/editCustomer', formatter: '.calcCreditState'}"
                                showValue="true" />
                        </VBox>
                    </HBox>
                </Panel>

            </VBox>
        </content>

        <beginButton>
            <Button
                text="{i18n>btnSaveChanges}"
                type="Emphasized"
                icon="sap-icon://save"
                press=".onSaveEdit" />
        </beginButton>
        <endButton>
            <Button
                text="{i18n>btnCancel}"
                type="Default"
                press=".onCancelEdit" />
        </endButton>

    </Dialog>

</core:FragmentDefinition>

